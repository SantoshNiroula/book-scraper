# Scrap Book

### Start Docker Container
First of all start docker
```bash
docker compose up -d
```

### Run the server
```bash
cd server
fastapi dev main.py
```

### Run the client application
```bash
cd client
flutter run
```